# setup.py

from setuptools import setup, find_packages

setup(
    name='example_package123',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[],
    entry_points={},
    classifiers=[
        'Programming Language :: Python :: 3',
        'License :: OSI Approved :: MIT License',
    ],
)
