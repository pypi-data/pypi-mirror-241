[tool.poetry]
name = "websocket-training"
version = "0.1.7"
description = "Websocket training project"
authors = ["deskent <battenetciz@gmail.com>"]
readme = "README.md"
packages = [
    { include = "websocket_training", from="src" }
]

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.104.1"
websocket = "^0.2.1"
uvicorn = "^0.24.0.post1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
