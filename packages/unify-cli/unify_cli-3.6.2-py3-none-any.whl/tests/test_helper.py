def build_command(values, content=None):
    if type(values) is dict:
        command = ""

        for key,value in values.items():
            pass

    raise Exception("Need a dictionary to build command")
