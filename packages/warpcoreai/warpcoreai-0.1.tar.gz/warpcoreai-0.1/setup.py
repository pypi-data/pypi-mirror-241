from setuptools import setup, find_packages

setup(
    name="warpcoreai",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # elenco delle dipendenze
    ],
    # altri metadati
)