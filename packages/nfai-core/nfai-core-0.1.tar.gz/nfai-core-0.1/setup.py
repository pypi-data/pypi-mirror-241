from setuptools import setup, find_packages

setup(
    name='nfai-core',
    version='0.1',
    author='NeuralFabric',
    description='Core library for NeuralFabric',
    packages=find_packages(),
    install_requires=[
        # List any dependencies your package requires
    ],
)